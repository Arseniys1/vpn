# Production Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# Docker Registry (Optional)
# ===========================================
REGISTRY=
VERSION=latest

# ===========================================
# Domain Configuration
# ===========================================
DOMAIN=example.com
ACME_EMAIL=admin@example.com

# ===========================================
# Database Configuration
# ===========================================
DB_USER=postgres
DB_NAME=xrayvpn

# Secrets (Don't set here - use Docker secrets)
# DB_PASSWORD will be set via Docker secret

# ===========================================
# RabbitMQ Configuration
# ===========================================
RABBITMQ_USER=xray_user
# RABBITMQ_PASSWORD will be set via Docker secret

# ===========================================
# Application Configuration
# ===========================================
APP_ENV=production
LOG_LEVEL=info

# ===========================================
# Service Replicas
# ===========================================
API_REPLICAS=3
WORKER_REPLICAS=2
FRONTEND_REPLICAS=2

# ===========================================
# Traefik Configuration
# ===========================================
TRAEFIK_LOG_LEVEL=INFO

# ===========================================
# Secrets (Set via Docker Secrets - NOT here)
# ===========================================
# TELEGRAM_BOT_TOKEN - Set via: echo "your_token" | docker secret create telegram_bot_token -
# JWT_SECRET - Set via: echo "your_secret" | docker secret create jwt_secret -
# DB_PASSWORD - Set via: echo "your_password" | docker secret create db_password -
# RABBITMQ_PASSWORD - Set via: echo "your_password" | docker secret create rabbitmq_password -
