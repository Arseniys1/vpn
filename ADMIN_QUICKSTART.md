# Быстрый старт: Админ-панель

## 🚀 Активация админ-доступа

### Шаг 1: Установите права администратора в базе данных

```sql
-- Подключитесь к вашей PostgreSQL базе данных
psql -U postgres -d xrayvpn

-- Назначьте права администратора пользователю по его Telegram ID
UPDATE users SET is_admin = true WHERE telegram_id = 123456789;
-- Замените 123456789 на ваш реальный Telegram ID
```

### Шаг 2: Перезапустите backend (если необходимо)

```bash
cd backend
go run cmd/api/main.go
```

### Шаг 3: Откройте приложение

1. Откройте Telegram Mini App
2. На главной странице появится кнопка **"Панель Администратора"** (оранжевая карточка)
3. Нажмите на неё для входа в админ-панель

## 📋 Основные возможности

### Вкладка "Статистика"
- Общее количество пользователей
- Активные подписки
- Доход за месяц
- Открытые тикеты
- Загрузка серверов

### Вкладка "Серверы"
**Добавление сервера:**
1. Нажмите "Добавить сервер"
2. Заполните форму:
   - Название: `DE-1`
   - Страна: `Германия`
   - Флаг: `🇩🇪` (используйте эмодзи)
   - Протокол: `vless` / `vmess` / `trojan`
   - Ping: `45`
   - Сообщение: `Высокоскоростной сервер!` (опционально)
3. Нажмите "Сохранить"

**Редактирование:**
- Нажмите "Редактировать" на карточке сервера
- Измените нужные поля
- Нажмите "Сохранить"

**Удаление:**
- Нажмите "Удалить"
- Подтвердите действие

### Вкладка "Пользователи"
- Поиск по имени или Telegram ID
- Просмотр баланса и статуса подписки
- Редактирование баланса
- Блокировка/разблокировка

### Вкладка "Тарифы"
**Создание тарифа:**
1. Нажмите "Добавить тариф"
2. Укажите:
   - Название: `3 Месяца`
   - Длительность: `3` (месяцы)
   - Цена: `250` (звезды)
   - Скидка: `-15%` (опционально)
3. Сохраните

### Вкладка "Тикеты"
- Просмотр всех обращений пользователей
- Ответ на тикеты
- Изменение статуса (Открыт/Отвечен/Закрыт)

## 🔒 Безопасность

- Доступ к админ-панели имеют только пользователи с флагом `is_admin = true`
- Все API запросы защищены middleware аутентификации
- Действия администраторов логируются

## 🛠️ API для разработчиков

База URL: `http://localhost:8080/api/v1/admin`

**Заголовки:**
```
X-Telegram-Init-Data: <telegram_init_data>
Content-Type: application/json
```

**Примеры запросов:**

```bash
# Получить статистику
curl -X GET http://localhost:8080/api/v1/admin/stats \
  -H "X-Telegram-Init-Data: ..."

# Создать сервер
curl -X POST http://localhost:8080/api/v1/admin/servers \
  -H "X-Telegram-Init-Data: ..." \
  -H "Content-Type: application/json" \
  -d '{
    "name": "US-WEST",
    "country": "США (Запад)",
    "flag": "🇺🇸",
    "protocol": "vless",
    "status": "online",
    "admin_message": "Новый сервер в Калифорнии!"
  }'

# Обновить пользователя
curl -X PUT http://localhost:8080/api/v1/admin/users/{user_id} \
  -H "X-Telegram-Init-Data: ..." \
  -H "Content-Type: application/json" \
  -d '{
    "balance": 5000,
    "is_active": true
  }'
```

## 📱 Скриншоты функций

### Управление серверами
- Добавление/редактирование серверов с флагами стран
- Установка сообщений администратора для каждого сервера
- Изменение статуса (Online/Maintenance/Crowded)

### Управление пользователями
- Поиск и фильтрация
- Редактирование баланса
- Управление подписками

### Статистика
- Визуализация загрузки серверов
- Ключевые метрики системы

## ⚠️ Важные заметки

1. **Первый запуск**: После миграции БД нужно вручную установить `is_admin = true` для первого администратора
2. **Безопасность**: Не давайте админ-права непроверенным пользователям
3. **Тестирование**: Протестируйте все функции на тестовых данных перед использованием в продакшене
4. **Backup**: Делайте резервные копии БД перед массовыми изменениями

## 🐛 Решение проблем

**Админ-панель не отображается:**
```sql
-- Проверьте статус администратора
SELECT telegram_id, first_name, is_admin FROM users WHERE telegram_id = 123456789;
```

**Ошибка 403 Forbidden:**
- Убедитесь, что `is_admin = true` в базе данных
- Проверьте логи backend на наличие ошибок аутентификации

**Изменения не сохраняются:**
- Проверьте Network вкладку в DevTools браузера
- Убедитесь, что backend запущен и доступен
- Проверьте логи сервера

## 📞 Поддержка

Полная документация: [`ADMIN_PANEL.md`](./ADMIN_PANEL.md)
