# 🚀 Быстрый запуск приложения (5 минут)

## Шаг 1: Запуск Backend

```bash
cd backend

# Установка зависимостей
go mod download

# Создание конфига (если нет)
cp configs/config.example.yaml configs/config.yaml

# Редактирование config.yaml
# Укажите свои данные PostgreSQL и Telegram Bot Token

# Запуск API
go run cmd/api/main.go
```

Backend будет доступен на `http://localhost:8080`

## Шаг 2: Запуск Frontend

```bash
# В корне проекта
npm install

# Запуск dev сервера
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

## Шаг 3: Настройка первого администратора

1. Откройте Telegram Mini App
2. Зарегистрируйтесь в приложении
3. Узнайте свой Telegram ID (отображается в профиле)
4. Выполните SQL команду:

```sql
UPDATE users SET is_admin = true WHERE telegram_id = YOUR_TELEGRAM_ID;
```

5. Перезайдите в приложение - появится кнопка "Панель Администратора"

## Шаг 4: Проверка работы

### Тестирование Admin Panel:

1. **Серверы:**
   - Нажмите "Добавить сервер"
   - Заполните форму (название, страна, флаг 🇩🇪, протокол)
   - Нажмите "Сохранить"
   - Убедитесь, что сервер появился в списке

2. **Пользователи:**
   - Откройте вкладку "Юзеры"
   - Найдите себя в списке
   - Нажмите "Редактировать"
   - Измените баланс
   - Сохраните

3. **Тарифы:**
   - Откройте вкладку "Тарифы"
   - Нажмите "Добавить тариф"
   - Создайте новый план подписки

4. **Тикеты:**
   - Откройте вкладку "Тикеты"
   - Создайте тестовый тикет от имени пользователя
   - Ответьте на него

## ✅ Что должно работать:

- ✅ Все кнопки кликабельны и работают
- ✅ Данные загружаются с сервера
- ✅ Изменения сохраняются в базу данных
- ✅ Показываются уведомления об ошибках/успехе
- ✅ Haptic feedback при действиях (в Telegram)

## ⚙️ Переменные окружения (опционально)

Вместо config.yaml можно использовать env variables:

```bash
export DB_HOST=localhost
export DB_PORT=5432
export DB_USER=postgres
export DB_PASSWORD=postgres
export DB_NAME=xrayvpn
export TELEGRAM_BOT_TOKEN=your_bot_token
export APP_ENV=development
```

## 🐛 Решение проблем

### Backend не запускается
```bash
# Проверьте PostgreSQL
psql -U postgres -c "SELECT version();"

# Проверьте порты
lsof -i :8080
```

### Frontend не подключается к API
- Проверьте `src/services/adminApi.ts` - правильный ли URL
- Убедитесь, что backend запущен на порту 8080
- Проверьте CORS настройки

### Админ панель не появляется
```sql
-- Проверьте статус админа
SELECT telegram_id, first_name, is_admin FROM users;

-- Установите is_admin
UPDATE users SET is_admin = true WHERE telegram_id = YOUR_ID;
```

## 📱 Telegram Bot Setup

1. Создайте бота через @BotFather
2. Получите токен
3. Добавьте в config.yaml или env variable
4. Настройте Mini App URL в BotFather

## 🎯 Готово!

Теперь у вас полностью рабочее приложение с:
- ✅ Панелью администратора
- ✅ Управлением серверами
- ✅ Управлением пользователями
- ✅ Системой подписок
- ✅ Системой поддержки

Все функции работают и готовы к использованию!

---

**Время запуска:** 5 минут
**Сложность:** Легко
**Результат:** Полностью рабочее приложение 🎉
